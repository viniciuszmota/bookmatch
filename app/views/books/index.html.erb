<h2>Meus Livros</h2>

<% @books.each do |book| %>
  <ul>
    <li><%= book.title %></li>
    <li><%= book.author %></li>
    <li><%= book.condition %></li>
  </ul>

  <% if book.photos.attached? %>
      <li>
        <%= image_tag book.photos.first, width: 300, height: 400, alt: "#{book.title} photo" %>
      </li>
    <% else %>
      <li><%= image_tag "no-image.jpg", width: 300, height: 400, alt: "Sem foto disponível" %></li>
    <% end %>

  <%= link_to "Editar Livro", edit_book_path(book.id) %> |
  <%= button_to 'Deletar', book_path(book), method: :delete %>
<% end %>

<%= link_to "Adicionar Livro", new_book_path %> |

<%= link_to "Matches", matches_path %> |

<%= link_to "Recusar", book_path(@book) %>
