<div class="white-container index-books-menu">
  <div class="icon-right">
    <%= link_to books_path do %>
      <%= image_tag "arrow-left.png", alt: "Voltar" %>
    <% end %>
  </div>

  <div class="logo-black" id="index-books-logo">
    <%= image_tag "logo-black.png", alt: "Logo BookMatch" %>
  </div>

  <div class="icon-left">
  </div>

  <%= link_to matches_path do%>
    <div class="icon-left" id="index-books-settings">
      <%= image_tag "settings.png", alt: "Configurações" %>
    </div>
  <%end%>

  <div class="main-image-avatar">
    <% if current_user.photo.attached? %>
      <%= image_tag current_user.photo, width: 100, height: 100 %>
    <% else %>
      <%= image_tag "no-image.jpg", width: 100, height: 100 %>
    <% end %>

  </div>
</div>

<div class="container" id="index-books-container">
  <div class="top-container container-image">
    <p class="top" id="index-book-username"><%= current_user.full_name.split.map(&:capitalize).join(' ') %></p>
  </div>

  <div class="top-general-container">
    <div class="top-white-container index-books-buttons">
      <div class="profile-button">
        <%= link_to books_path, class: "profile-button-my-books-link" do %>
          <button id="profile-button-my-books" class="profile-button-my-books">
            <%= image_tag "my-books.png", alt: "Meus Livros", class: "icon-above" %>
            Meus Livros
          </button>
        <% end %>
        <%= link_to new_book_path, class: "profile-button-my-books-link" do %>
          <button id="profile-button-my-books" class="profile-button-my-books">
            <%= image_tag "my-books.png", alt: "Cadastrar Livro", class: "icon-above" %>
            Cadastrar
          </button>
        <% end %>
        <%= link_to matches_path, class: "profile-button-matches-link" do %>
          <button id="profile-button-matches" class="profile-button-my-books">
            <%= image_tag "matches.png", alt: "Matches", class: "icon-above" %>
            Matches
          </button>
        <% end %>
      </div>
    </div>

    <div class="book-container-general">
      <% @books.each do |book| %>
        <%= link_to edit_book_path(book), class: "book-container" do %>
          <div class="book-button-photo">
            <% if book.photos.attached? %>
              <li>
                <%= image_tag(book.photos.first, crop: :fill, alt: "#{book.title} photo") %>
              </li>
            <% end %>
          </div>
          <div class="book-button-info">
            <ul>
              <li><%= book.title %></li>
              <li><%= book.author %></li>
              <li><%= book.condition %></li>
            </ul>
          </div>
        <% end %>
      <% end %>

      <%= link_to new_book_path(:user), class: "add-book-button" do %>
        <span>Adicionar livro</span>
      <% end %>
    </div>
  </div>
</div>
